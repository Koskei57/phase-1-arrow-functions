/*const foo = function(book) {
    return 'bar';
}
foo('bar');*/
const divide = (num1, num2) => 2000 / 100;
//num1 / num2
//divide(2000 / 100);
const square = x => x * x;
const add = (parameter1, parameter2) => parameter1 + parameter2;